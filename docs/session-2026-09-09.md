# WildDragons session — 2026-09-09

## Requested work
- Flow trailer for TikTok, YouTube Shorts and YouTube, connecting Dragon Arena branding to WildDragons.
- Repair authentication at /connect using the existing GitHub, Vercel and Supabase setup.
- Retain this work with the WildDragons project.

## Findings
- The Supabase client, connect form actions, password reset and route guard were commented out.
- Vercel production deployment dpl_4QBnuXtJ9uSNvzxcaNmwiQa4hnYR failed because src/stores/user.js imported a nonexistent Supabase export from src/main.js.
- The connected Supabase account currently lists only appforge. Its relationship to WildDragons has not been established; no project/database was repurposed.
- Vercel project wild-dragons is accessible, but its returned domain list does not contain wild-dragons.vercel.app. Confirm the intended production alias before publishing a campaign.

## Changes
- Shared Supabase client with publishable-key or legacy anon-key configuration.
- Restore email/password sign-in, confirmation-aware registration, Google OAuth, password recovery and session-aware account/logout controls.
- Verified-user route guards and internal-only return paths.
- SPA rewrites for direct routes and OAuth/recovery returns.
- Dragon Arena branding copy and landing-page link to /connect.
- Flow storyboard in branding/dragon-arena-flow.md.

## Deployment requirements
1. Identify the existing WildDragons Supabase project; do not use appforge without establishing that it is intended.
2. Configure VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY (or existing VITE_SUPABASE_ANON_KEY) in Vercel for the target deployment.
3. In that Supabase project's Auth URL configuration, set the actual production Site URL and allow /world and /reset-password return URLs for each approved origin. Add other protected return routes if they are used after OAuth/signup.
4. Enable Google Auth with the project's OAuth client; register the Supabase callback URL in Google Cloud.
5. Deploy and verify sign-in, confirmation email, Google callback, recovery, refresh, logout and protected routes against the actual backend.

The current change links the branding narrative and CTA only. It does not migrate or synchronize Dragon Arena sessions, turns, galleries or identities across applications. That requires the source app and an agreed ownership/data mapping.

The chat itself cannot be moved into a ChatGPT Project through the available tools. This document preserves the task summary in the repository.
